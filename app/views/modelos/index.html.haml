%h1 Listing modelos

%table
  %thead
    %tr
      %th Modelo
      %th Activo
      %th
      %th
      %th

  %tbody
    - @modelos.each do |modelo|
      %tr
        %td= modelo.modelo
        %td= modelo.activo
        %td= link_to 'Show', modelo
        %td= link_to 'Edit', edit_modelo_path(modelo)
        %td= link_to 'Destroy', modelo, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Modelo', new_modelo_path
